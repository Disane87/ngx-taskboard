
dist: trusty
sudo: false

language: node_js
node_js:
  - '8'

cache:
  directories:
    - ./node_modules

install:
  - npm install 

stages:
  - name: Build

jobs:
  include:
    - stage: Build
      if: branch = master      
      script:
        - npm run semantic-release:run